import BaseState from '../../../states/BaseState'

export class BasketEmptyState extends BaseState {
    public enter(scene: Phaser.Scene, basket: Phaser.Physics.Arcade.Sprite) {
        return
    }
    public execute(scene: Phaser.Scene, basket: Phaser.Physics.Arcade.Sprite) {
        return
    }
}
